# E8D 登入前版型 — 設計規範

> 建立日期：2026-07-30
> 樣式基準：`F1登入頁-提案.html`、`F1-2FA綁定引導-提案.html`
> Token 實作：`auth-tokens.css`（新頁面一律 link 這支，不要複製變數區塊）
> Source of truth：Figma「E8D Design System」`nSdZyWgZ1VPFtVQ4hqLWbE`

---

## 為什麼有這份文件

登入前這批頁面每一頁都自己複製一份 `:root` 變數區塊，改到第三、四頁就開始各自漂移。
2026-07-30 清查基準頁時發現，**兩個基準頁彼此之間就已經有 5 處不一致**（見文末）。

解法是把 token 抽成單一檔案 `auth-tokens.css`，新頁面 link 它即可，
不再有「這頁的灰跟那頁的灰不一樣」的問題。

---

## 一、色彩語意對照

| 語意 | Token | 值 | 用在哪 |
|---|---|---|---|
| **primary**（主色／CTA） | `--primary-700` | `#FF6416` | 主要按鈕實心底、focus 邊框、步驟指示器當前與已完成、橘色 icon |
| primary hover | `--primary-800` | `#FB5C13` | 主按鈕 hover 滑入色 |
| primary pressed | `--primary-900` | `#F04D0A` | 主按鈕按下 |
| 品牌淺底 hover | `--bg-brand-hover` | `#FFEADE`（primary/75） | 橘色 icon／文字按鈕的 hover 底色 |
| **error**（錯誤／鎖定） | `--text-error` | `#EC2D30`（error/700） | 帳密錯誤、驗證碼錯誤、已鎖定 |
| error 邊框 | `--border-error` | `#F64C4C`（error/600） | 錯誤欄位外框 |
| **warning**（警告） | `--text-warning` | `#FE9B0E`（warning/700） | 最後一次機會、今日次數上限、逾時 |
| **success**（成功） | `--text-success` | `#0C9D61`（success/700） | 已重新發送、規則達成、完成頁 |
| success 圖示／圓 | `--icon-success` | `#47B881`（success/600） | 綠勾圖示、完成頁大圓 |
| **text-secondary**（輔助灰字） | `--text-secondary` | `#79706A`（neutral/500） | 副標、說明、次要連結 |
| 文字主色 | `--text-primary` | `#2C2324`（neutral/700） | 標題、輸入值 |
| 文字停用 | `--text-disabled` | `#A79E97`（neutral/400） | placeholder、未達成的規則 |

### 命名：顏色 token 用 error，元件的破壞性類型仍叫 Danger

**2026-07-30 已把 Figma 的 11 個 `danger` 變數改名為 `error`**（primitive 4 個＋semantic 7 個），
Figma 與程式碼端現在都是 `error`。

但 **Button／Badge／Modal 的 `Danger` variant 名稱維持不變**，因為那不是顏色名，
而是「破壞性操作」的語意（Button 文件原文：「Danger 用於破壞性操作（如刪除），
要讓使用者停下來注意。」）。按鈕不是「錯誤按鈕」，改名語意會跑掉。

| 層級 | 名稱 | 意思 |
|---|---|---|
| 顏色 token | `error` | 錯誤狀態的紅（表單錯誤、鎖定訊息） |
| 元件 variant | `Danger` | 破壞性操作（刪除按鈕、危險確認 Modal） |

破壞性操作的元件會綁 `color/bg/error-solid` 這組 token —— 這是刻意的，兩者共用同一組紅色。

### 紅色與黃色怎麼分（2026-07-29 定案）

- **紅色 = 已經出事**：帳密錯誤、驗證碼錯誤、帳號已鎖定
- **黃色 = 再一步就出事**：只剩 1 次機會、今日索取次數已達上限

---

## 二、間距、圓角、字級

**間距**（4px grid）：`--space-xs 4` / `sm 8` / `md 12` / `lg 16` / `xl 24` / `2xl 32` / `3xl 48`

**圓角**：`--radius-sm 4` / `md 8` / `lg 12` / `xl 20`（卡片） / `full 999`
＋ **`--radius-field 16`**（登入前版型專用：輸入欄位與按鈕）

**字級**：

| Token | 值 | 用途 |
|---|---|---|
| `--fs-hero` | 30px | 左側視覺主標 |
| `--fs-title` | 24px | 卡片標題 h1 |
| `--fs-lead` | 17px | 強調值（帳號、手機號） |
| `--fs-body` | 15px | 輸入值、主按鈕 |
| `--fs-sm` | 14px | 次要按鈕、footer 連結 |
| `--fs-caption` | 13px | 副標、訊息、說明 |
| `--fs-micro` | 12.5px | 註記、規則清單 |
| `--fs-label` | 12px | floating label 上移後 |
| `--fs-otp` | 22px | 驗證碼大數字 |

**字重**：`--fw-regular 400` / `medium 500`（按鈕、標籤） / `semi 600`（強調值） / `bold 700`（標題）

**版型尺寸**：左視覺區 `44%`、卡片 `400px`（寬版 `520px`）、欄位高 `56px`、主按鈕 `48px`、次要按鈕 `44px`

> ⚠️ 欄位 56px、`radius-field` 16px、卡片圓角 20px 目前**還沒收進 Figma**
> （Figma 的 Input 最高 40、Radius 只有 4/8/12/20）。
> 2026-07-29 決定等登入前這批頁面全定案後一次收進去。

---

## 三、元件慣例

| 元件 | 規格 |
|---|---|
| **卡片** | 白底、`radius-xl` 20px、`shadow-lg`、內距 `32px 24px`、置中、標準寬 400px。class 一律用 `.auth-card`（寬版加 `.is-wide`） |
| **主要按鈕** | 橘色實心、白字、48px、`radius-field`。hover 是**由左滑入**較深橘（不是單純換色）、pressed 縮放 .98、disabled 灰底灰字 |
| **次要按鈕** | 白底＋灰外框、深色字、44px。hover 換 `--bg-hover` 底並加深邊框 |
| **橘色 icon 按鈕** | 無底無框、橘色 icon、32px。hover 配**淺橘底** `--bg-brand-hover`，**不可用中性灰**（橘配暖灰會髒） |
| **表單** | floating label：未輸入時標籤置中，focus／有值時上移縮小。focus 用橘邊框＋外部光暈 `--focus-ring`（不只換邊框色） |
| **錯誤／警告訊息** | **icon ＋ 文字一行、無框無底色**，圖示與文字同色。緊接在相關欄位下方 |
| **提示框 callout** | 只有「需要整段說明」時才用有底色的框（如 warning 淺黃底）。單行訊息一律用上面的 `.msg` |
| **Stepper** | 圓點 28px、線 `flex:1` 撐滿、當前與已完成都是橘色、已完成顯示打勾並隱藏數字、未進行是米灰底（`neutral-100` ＋ `border-default`） |
| **資訊顯示框** | 唯讀值（帳號、綁定手機）用 `.info-box`：淺灰底＋橘色 icon＋小標＋17px 粗體值。**底色不用主色**，橘色留給主按鈕當唯一焦點 |
| **結果頁** | 置中大圓（88px）＋標題＋說明。成功用 success 綠、逾時用 warning 黃 |

### Stepper 有沒有文字標籤

兩種都是對的，依情境選，但**圓點與線的規格必須固定**：

- **帶標籤**：首次設定引導這類「使用者需要預先知道全程要做什麼」的流程（如 2FA 綁定引導）
- **只有圓點**：一次性的短流程，標題已經說明當前步驟（如忘記密碼）。
  這種情況放標籤會變成「同一件事講兩次」

### ✅ 已完成步驟用橘色打勾（2026-07-30 Alvie 定案）

需求原本寫「已完成綠色打勾」，最後**定案維持橘色**（甲案）。

理由：綠色打勾會讓 stepper 同時出現橘（當前）＋綠（已完成）＋米（未進行）三個色系，
違反「同一元件不超過兩個色系」的原則。維持橘色後只有**主色橘（當前＋已完成）
＋中性米灰（未進行）**兩色，已完成與當前的區別改用**打勾取代數字**來表達，
不必再多一個顏色。

比較用的 `Stepper元件-提案.html`（甲乙並排）保留備查，乙案（綠色）不採用。

---

## 四、硬規則

1. **新頁面一律 `<link rel="stylesheet" href="auth-tokens.css">`**，不可再複製 `:root` 變數區塊到頁面內
2. **不可硬填色碼或 px**，一律用 token。頁面內只放該頁獨有的版面樣式
3. **系統裡沒有的樣式 ＝ 設計系統缺口**，要先講明並提出選項（甲：用既有樣式替代／乙：擴充系統），
   不可自己刷一個新樣式帶過
4. **Figma 是 source of truth**，`auth-tokens.css` 的值必須與 Figma Variables 對齊；
   兩邊不一致時以 Figma 為準
5. 交稿前跑一次視覺層次自我檢查（主角權重／有無重複講／間距分組／顏色數量）

---

## 五、清查出的 5 處不一致與裁定

2026-07-30 比對兩個基準頁時發現以下差異，已在 `auth-tokens.css` 裡收斂成單一標準：

| 項目 | 登入頁 | 2FA 綁定引導 | 裁定 | 理由 |
|---|---|---|---|---|
| 左側視覺區寬度 | 44% | **40%** | **44%** | 登入頁與 2FA 驗證頁都是 44%，2 比 1 |
| 卡片標題 h1 | 24px | **22px** | **24px** | 登入頁與 2FA 驗證頁都是 24px |
| 副標 subtitle | 13px | **14px** | **13px** | 登入頁與 2FA 驗證頁都是 13px |
| logo 下方間距 | 24px | 24px | **24px** | （2FA 驗證頁是 16px，屬少數） |
| 卡片 class 名稱 | `.login-card` | `.wizard-card` | **`.auth-card`** | 同一個東西不該有三個名字（2FA 驗證頁叫 `.auth-card`） |

另外 `--bg-hover` 原本三頁都誤寫成 `neutral-100`，實際 Figma 綁的是 `neutral-50`
（`neutral-100` 是 `bg/pressed`）——已於 2026-07-30 修正，這也是「那個灰看起來偏棕」的原因。

**本次不動任何既有頁面**（依 Alvie 指示）。之後要不要回頭套用這份規範另行決定。
